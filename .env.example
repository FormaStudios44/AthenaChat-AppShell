# Copy to .env.local and fill in your key.
# Required for direct browser → Anthropic API calls (prototype mode only).
# In production, proxy this through a backend route.
VITE_ANTHROPIC_API_KEY=sk-ant-...
